# Tinder 规则集 (by Grok, 2026-01-14)
# 用于 Loon/Shadowrocket，直连或代理优化
# 优先级：精确域名 > 子域名

DOMAIN,images-ssl.gotinder.com,DIRECT  # 图片直连优化
DOMAIN,api.gotinder.com,DIRECT        # API 直连
DOMAIN,profile.gotinder.com,DIRECT    # 资料
DOMAIN,auth.gotinder.com,DIRECT       # 登录

DOMAIN-SUFFIX,gotinder.com,DIRECT     # 兜底 gotinder
DOMAIN-SUFFIX,tinder.com,DIRECT       # 兜底 tinder

# 可选: 如果图片仍慢，临时加 CDN（风险: 影响其他 App）
# DOMAIN-SUFFIX,cloudfront.net,DIRECT
